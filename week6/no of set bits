#include <iostream>

using namespace std;

int main()
{
    int n;
    int count=0;
    cin>>n;
    while(n)
    {
       count++;
       n=n&(n-1);
    }

    cout<<"No. of set bits "<<count;
    return 0;
}
